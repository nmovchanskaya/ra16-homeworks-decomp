/**
 * Exchange Wiget shows rates of exchange
 * has next params:
 *
 * @param {ExchangeRateType[]} rates
 */

export const ExchangeWiget = ({rates}: any) => {
    return (
        <>
            {rates}
        </>
    )
}
