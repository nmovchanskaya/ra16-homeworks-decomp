/**
 * TV Widget shows all the TV programs, that it gets
 * has next params:
 *
 * @param {TVProgramType[]} programs
 */

export const TVWidget = ({programs}: any) => {
    return (
        <>
            {programs}
        </>
    )
}
