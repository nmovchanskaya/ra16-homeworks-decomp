/**
 * Search widget searches for the links with set category
 * has next params:
 *
 * @param {string} category
 */

export const SearchWidget = ({category}: any) => {
    return (
        <>
            {category}
        </>
    )
}
