/**
 * Banner Widget shows ad banner
 * has next params:
 *
 * @param {string} img
 * @param {string} link
 */

export const BannerWidget = ({img, link}: any) => {
    return (
        <>
            {img}{link}
        </>
    )
}
