/**
 * Popular Item shows one popular item
 * has next params:
 *
 * @param {string} title
 * @param {string} link
 */

export const PopularItem = ({title, link}: any) => {
    return (
        <>
            {title}{link}
        </>
    )
}
