/**
 * Popular Widget shows all the popular items, that it gets
 * has next params:
 *
 * @param {PopularItemType[]} pops
 */

export const PopularWidget = ({pops}: any) => {
    return (
        <>
            {pops}
        </>
    )
}
